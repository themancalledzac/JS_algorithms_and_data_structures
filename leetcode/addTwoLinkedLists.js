/**
 * Definition for singly-linked list.
 * function ListNode(val, next) {
 *     this.val = (val===undefined ? 0 : val)
 *     this.next = (next===undefined ? null : next)
 * }
 */
/**
 * @param {ListNode} l1
 * @param {ListNode} l2
 * @return {ListNode}
 */
var addTwoNumbers = function (l1, l2) {
  // let node1 = variable for first node of l1
  for (node of l1) {
      
  } 
  // let node2 = variable for first node of l2
  // add node1 and node2
  //      // if addition is greater than or equal to 10
};

console.log(addTwoNumbers([2, 4, 3], [5, 6, 4]));
